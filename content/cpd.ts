import type { CpdProvider } from './types';

// PLACEHOLDER — the Council has not accredited any CPD providers, and has
// not published a points requirement, a cycle, or the categories of activity
// that count.
//
// The Act establishes THAT CPD is required (Art. 44(2)(c), 34(5)(b)(i)). It
// does not set the number. DO NOT INVENT ONE: a professional who does 20
// points because this page said 20 and then fails renewal because the real
// figure was 30 has been harmed by us, not by the Council.
//
// Launches empty. Add a provider here and the table renders.
export const cpdProviders: CpdProvider[] = [];
